# Interactive Demo Request Template

**Task**: Create an interactive prototype for the [Component Name] designed in the architecture.

**Requirements**:
- Use React + Tailwind CSS.
- Visualize the data flow or user interaction described in [Mermaid Diagram].
- Make it interactive (buttons, sliders, data entry).
- Purpose: Demonstrate [Key Feature/Flow] to the stakeholder.

**Output**:
- A single HTML artifact (using `web-artifacts-builder` or `frontend-design`).
