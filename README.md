# 🛠️ cruldra-skills

这是我个人开发和维护的 Agent Skills（AI 智能体技能）集合仓库。这里的技能旨在扩展各类 AI Coding Agent（如 **OpenCode**, **Claude Code**, **Cursor**, **Windsurf**, **Roo Code** 等）的能力，为其提供特定的领域知识、工作流和外部工具集成。

目前仓库中包含的技能：
- **tauri-v2**: Tauri v2 应用开发的最佳实践与工作流指南。
- **xmind**: 通过 Python 脚本对 XMind 思维导图进行程序化解析、操作和生成的技能。

## 🚀 如何使用 `skills` CLI 安装

你可以通过开源生态的 [`skills` CLI](https://github.com/vercel-labs/skills) 来直接获取和安装本仓库中的技能。

> **注意：** 如下命令假设本仓库托管在 GitHub `cruldra/skills`。如果地址不同，请将 `cruldra/skills` 替换为实际的完整 Git URL（例如 `https://github.com/your-username/skills`）。

### 1. 🔍 查看可用技能列表

在安装前，你可以使用 `--list` 参数查看本仓库中目前有哪些技能：

```bash
npx skills add cruldra/skills --list
```

### 2. 📦 安装特定技能

使用 `-s` 或 `--skill` 参数安装你需要的某个特定技能（例如 `tauri-v2` 或 `xmind`）：

```bash
# 将 tauri-v2 技能安装到当前项目的 Agent 目录中
npx skills add cruldra/skills -s tauri-v2

# 同时安装多个特定技能
npx skills add cruldra/skills -s tauri-v2 -s xmind
```

### 3. 🌐 全局安装 (Global)

如果你希望在你电脑上的所有项目中都能让 Agent 使用这些技能，可以添加 `-g` 参数进行全局安装：

```bash
npx skills add cruldra/skills -s xmind -g
```

### 4. 🤖 指定安装到特定的 Agent

CLI 会自动检测你当前环境中的 Agent。如果你想明确指定安装到某个 Agent（例如仅安装给 OpenCode 或 Claude Code），可以使用 `-a` 参数：

```bash
npx skills add cruldra/skills -s tauri-v2 -a opencode -a claude-code
```

### 5. ⚡ 一键安装所有技能

如果你想一次性安装本仓库的所有技能，可以使用 `--all` 参数（此操作跳过交互式确认）：

```bash
npx skills add cruldra/skills --all
```

---

## 🛠️ 管理已安装的技能

`skills` CLI 同样提供了便捷的命令来管理你已经安装的技能：

- **查看本地已安装的技能：**
  ```bash
  npx skills list
  ```
- **检查更新并升级到最新版本：**
  ```bash
  npx skills check
  npx skills update
  ```
- **移除不再需要的技能：**
  ```bash
  npx skills remove tauri-v2
  ```

## 📚 了解更多

要了解关于 Agent Skills 规范的更多内容，或探索其他社区技能，请访问 [skills.sh](https://skills.sh) 或是查阅 [Agent Skills 规范](https://agentskills.io)。